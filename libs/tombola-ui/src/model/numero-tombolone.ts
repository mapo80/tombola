export class NumeroTombolone {
    public numero: number;
    public isEstratto: boolean;

    constructor (numero: number, isEstratto: boolean) {
        this.numero = numero;
        this.isEstratto = isEstratto;
    }
}